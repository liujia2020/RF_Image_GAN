# cGAN Configs

Store reusable base configs here.

Formal runs must copy a config into their own run folder as `config.yaml` before training starts.

Do not edit a run's `config.yaml` after training starts. Create a new run instead.
