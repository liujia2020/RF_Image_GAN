# 当前任务

> 给 Codex。现在要做什么。最后更新: 2026-06-11。

## ✅ 已完成

| 任务 | 日期 |
|------|------|
| 全量 fp16 cache 生成与验证 | 2026-06-07 |
| Phase 2a 损失健康检查（v0~v3 四轮消融） | 2026-06-08 |
| 路线转向决策（复数→B-mode） | 2026-06-09 |
| B-mode GT 离线转换 1500 个 + Proma 验证 | 2026-06-10 |
| 第十八工作单元：B-mode 纯监督基线（TinyBModeRFNet） | 2026-06-10 |
| TinyBModeRFNet 失败诊断 | 2026-06-11 |
| Light3DUNet 实现规格（Claude/Proma 联合） | 2026-06-11 |

### 第十八工作单元结果（失败，作为诊断输入）

- 网络：TinyBModeRFNet (260K 参数)
- 结果：train SSIM 0.131→0.267，val SSIM 0.138→**0.098（反降）**
- 诊断：pred_mean 卡在 0.528（灰雾），感受野仅 5 体素，严重欠拟合
- 根因：无下采样/跳跃连接、容量不足、感受野太小

## 🔴 当前任务：第十九工作单元 — Light3DUNet 实现与训练

**状态**：规格已就绪，等 Codex 实现。

**目标**：用 Light3DUNet（轻量 3D U-Net，~2.11M 参数）替换 TinyBModeRFNet，跑 50 epoch 纯监督训练。核心看点：pred 能否脱离 baseline、SSIM 稳定上升。

**详细规格**：见 `workspace-files/.context/codex_new_session_brief.md`（自包含，Codex 新对话直接使用）。

**改动范围**（最小化）：

| # | 改动 | 文件 |
|---|------|------|
| 1 | 新增 `Light3DUNet` + `DoubleConv3D` 类 | `rf_cgan_models.py` |
| 2 | 改 import：`TinyBModeRFNet` → `Light3DUNet` | train.ipynb |
| 3 | 改 batch：`samples_per_category=1` → `2` | train.ipynb |

**核心规格摘要**：

- 架构：3 级 encoder-decoder + 跳跃连接，入口 Conv3d(1536→64,k1)
- 通道：64→48→56→64（encoder），各向异性下采样
- BN momentum=0.9，output bias 初始化=-2.0（防灰雾）
- batch=6（每类 2 个），其余超参不变

**数据路径**（不变）：

| 数据 | 路径 |
|------|------|
| 输入(RF) | `/home/liujia/rf_training_cache/cgan_phase2a_64x32x32_random_full1500_fp16_cache_20260607/` |
| B-mode GT | `/home/liujia/rf_training_cache/cgan_phase2a_64x32x32_bmode_gt_ref64407p58_20260610/` |

## 环境

conda env: rf-cgan-clean | python: 3.10 | torch: 2.11.0+cu128 | GPU: RTX 5060 (8GB)
工作目录: `/media/liujia/8CC24D13C24D02C6/code/RF_Image_GAN/`
