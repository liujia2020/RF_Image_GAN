# 代码地图

> 给 Codex。代码结构详见 `项目笔记/03_代码架构.md`。
> 本文档只列当前有效路径，不重复架构说明。最后更新: 2026-06-11。

## 工作目录

```
/media/liujia/8CC24D13C24D02C6/code/RF_Image_GAN/
```

## 生产代码速查

| 文件 | 角色 | 状态 |
|------|------|------|
| `rf_learning_dataset.py` | 读 H5 → tensor | ✅ 不变 |
| `rf_cache_builder.py` | H5 → fp16 cache | ✅ 不变 |
| `rf_cached_dataset.py` | 读 cache → tensor | 🔄 新增读 B-mode GT |
| `rf_cgan_models.py` | 生成器 | 🔄 **本次改动**：新增 `Light3DUNet` + `DoubleConv3D`（~2.11M 参数 3D U-Net），输出 [1,64,32,32]+sigmoid |
| `rf_cgan_losses.py` | 旧损失 | ✅ 已废弃（新损失在训练脚本） |
| `rf_cgan_data.py` | Sampler | ✅ 不变 |

## 当前数据路径

| 数据 | 路径 |
|------|------|
| fp16 cache (RF) | `/home/liujia/rf_training_cache/cgan_phase2a_64x32x32_random_full1500_fp16_cache_20260607/` |
| B-mode GT | `/home/liujia/rf_training_cache/cgan_phase2a_64x32x32_bmode_gt_ref64407p58_20260610/` |

## 关键约束

- cache: normalize=True + restore_scale=True
- 网络输出: 单通道 + sigmoid → [0,1]
- 损失: 0.84*(1-SSIM3D) + 0.16*L1，pytorch-msssim 3D，window=7
- 训练: BN（momentum=0.9），AMP关，num_workers=0，分层batch（每类2个=6 total），全量验证，禁用对抗/判别器
